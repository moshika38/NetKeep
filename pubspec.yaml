name: netkeep
description: ""

publish_to: "none"

version: 1.0.1+2

config_name:
  name: "NetKeep"

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  rename_app: ^1.6.6
  flutter_dotenv: ^6.0.1
  google_mobile_ads: ^9.1.0
  permission_handler: ^11.4.0
  shared_preferences: ^2.5.3
  usage_stats: ^2.0.1
  package_info_plus: ^10.2.1
  dio: ^5.11.0
  flutter_background_service: ^5.1.0
  netkeep_traffic_stats:
    path: packages/netkeep_traffic_stats

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/app_icon.png
