name: netkeep
description: ""

publish_to: "none"

version: 1.0.0+1

config_name:
  name: "NetKeep"

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  rename_app: ^1.6.6
  http: ^1.6.0
  intl: ^0.20.3
  wakelock_plus: ^1.7.0
  flutter_dotenv: ^6.0.1
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/app_icon.png